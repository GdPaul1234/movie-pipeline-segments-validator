package com.gdpaul1234.movie_pipeline_segments_validator_ui.medias.presentation.util

/**
 * Different type of navigation supported by app depending on device size and state.
 */
enum class MediasNavigationType {
    NAVIGATION_BAR,
    NAVIGATION_DRAWER,
    NAVIGATION_RAIL,
}

/**
 * Different position of navigation content inside Navigation Rail, Navigation Drawer depending on device size and state.
 */
enum class MediasNavigationContentPosition {
    CENTER,
    TOP,
}
